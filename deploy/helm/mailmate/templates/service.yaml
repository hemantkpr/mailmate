apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailmate.fullname" . }}
  labels:
    {{- include "mailmate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mailmate.selectorLabels" . | nindent 4 }}
